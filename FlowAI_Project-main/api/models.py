# The `api` app is a routing/serialization layer over the models defined
# in accounts, monitoring, signals_app, prediction, analytics,
# notifications, and reports. It intentionally owns no models of its
# own. Serializers, viewsets, and urls.py are added in a later phase.
